name: CI/CD

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

permissions:
  contents: read
  packages: write

jobs:
  pipeline:
    uses: ArchGenTf/Main/.github/workflows/backend.yml@master
    with:
      service: "project-service"
      image: "acrarchgen.azurecr.io/project-service"
      trigger: ${{ github.event_name }}
    secrets: inherit
